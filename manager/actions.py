

class action:
    def __init__(self, classes):
        action="no class"
        classes=self.classes
        step=["action","valisation","stop"]
        # self.tricks = []

    def add_action(self, action):
        self.action=next_action(action)

    def next_action(current_action):
        if current_action==self.classes[-1] or current_action==self.classes[-2] :

